# GUI Textures

Place `background.png` in this directory to use it as the custom title screen background.

- Recommended size: **1920x1080** (or any resolution matching your target aspect ratio)
- Format: **PNG**
- The texture is referenced via the identifier `alohaclient:textures/gui/background.png`

If no background texture is present, the animated gradient background will be rendered instead.
